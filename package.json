{
  "name": "your-app-name",
  "version": "1.0.0",
  "private": true,
  "homepage": "https://kush-3.github.io/toDoList",
  "dependencies": {
    "react-scripts": "^5.0.1"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d build",
    "test": "react-scripts test",
    "eject": "react-scripts eject"
  }
}
